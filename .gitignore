# Dependencies
node_modules/
.pnpm-store/

# Next.js Build Output
.next/
out/
build/
dist/

# Rust / Anchor Build Artifacts
target/
**/*.rs.bk

# Environment Variables & Secrets
.env
.env*.local
*.env

# IDE Agent Guidelines (Excluded per user request)
AGENTS.md
CLAUDE.md

# Documentation (Excluded per user request)
README.md
web/README.md

# Logs & OS Files
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# Solana Local Validator Ledger
test-ledger/

# Anchor upgrade buffer keypair (auto-generated, not needed in git)
target/deploy/*-upgrade-buffer.json

# Solana Keypairs & Secrets (CRITICAL SECURITY)
**/*.json
!package.json
!package-lock.json
!tsconfig.json
!jsconfig.json
!next.config.json
!**/*/novachain.json
!**/Cargo.lock
!**/Anchor.toml
*.key
*.pem

# Next.js build and cache inside subfolders
web/.next/
web/out/
web/node_modules/
.next/
node_modules/
